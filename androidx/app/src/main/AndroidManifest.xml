<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dxn.agventure">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AgventureApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agventure">
        <activity
            android:name=".features.customer.CustomerActivity"
            android:exported="false" />
        <activity
            android:name=".features.seller.SellerActivity"
            android:exported="false" />
        <activity
            android:name=".features.consumer.ConsumerActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>